# Cloudflare Pages / Netlify cache rules.
# Photo files have content-hashed names, so they can cache forever.
/photos/*
  Cache-Control: public, max-age=31536000, immutable

# Vite build assets are also content-hashed.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
